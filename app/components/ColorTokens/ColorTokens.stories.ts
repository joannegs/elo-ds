import { Meta } from '@storybook/react';
import ColorTokenDoc from './ColorTokens.mdx';

export default {
  title: 'Tokens/Color',
  parameters: {
    docs: {
      page: ColorTokenDoc,
    },
  },
} as Meta;
