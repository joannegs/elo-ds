import { Meta } from '@storybook/react';
import RadiusTokenDoc from './Radius.mdx';

export default {
  title: 'Tokens/Border Radius',
  parameters: {
    docs: {
      page: RadiusTokenDoc,
    },
  },
} as Meta;
